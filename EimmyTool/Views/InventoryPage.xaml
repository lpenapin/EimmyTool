<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EimmyTool.Views.InventoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EimmyTool.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid Padding="20">
        <StackPanel Spacing="12">

            <TextBlock
                Text="Inventario"
                FontSize="26"
                FontWeight="SemiBold"/>

            <controls:DataGrid
                x:Name="InventoryGrid"
                AutoGenerateColumns="False"
                IsReadOnly="True">

                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="Producto" Binding="{Binding Name}" Width="*" />
                    <controls:DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity}" Width="100" />
                    <controls:DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="*" />
                    <controls:DataGridTextColumn Header="Costo Prom" Binding="{Binding Cost}" Width="120" />
                    <controls:DataGridTextColumn Header="Precio Prom" Binding="{Binding Price}" Width="120" />
                </controls:DataGrid.Columns>

            </controls:DataGrid>

        </StackPanel>
    </Grid>
</Page>
