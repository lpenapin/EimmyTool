<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="EimmyTool.Views.DebtPaymentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EimmyTool.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:EimmyTool.Converters"
    mc:Ignorable="d"
    Title="Procesar Pago"
    PrimaryButtonText="Pagar"
    SecondaryButtonText="Cancelar"
    DefaultButton="Primary">
    <ContentDialog.Resources>
        <converters:CurrencyFormatConverter x:Key="CurrencyConverter"/>
    </ContentDialog.Resources>

    <StackPanel Spacing="12" Margin="0,10,0,0">
        <StackPanel Orientation="Horizontal" Spacing="5">
            <TextBlock Text="Nombre:" FontWeight="Bold"/>
            <TextBlock x:Name="ClientNameText" Text="{x:Bind Client.Name, Mode=OneWay}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Spacing="5">
            <TextBlock Text="DNI:" FontWeight="Bold"/>
            <TextBlock x:Name="ClientDniText" Text="{x:Bind Client.DNI, Mode=OneWay}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Spacing="5">
            <TextBlock Text="Deuda Actual:" FontWeight="Bold"/>
            <TextBlock x:Name="CurrentDebtText" />
        </StackPanel>

        <MenuFlyoutSeparator/>

        <Button Content="Pagar Total" 
                Click="PayFull_Click" 
                HorizontalAlignment="Right" 
                Style="{StaticResource AccentButtonStyle}"/>

        <NumberBox x:Name="PaymentInput"
                   Header="Valor a pagar"
                   PlaceholderText="$0.00"
                   Value="{x:Bind PaymentAmountDouble, Mode=TwoWay}"
                   ValueChanged="PaymentInput_ValueChanged"
                   Minimum="0" 
                   Maximum="{x:Bind (x:Double)Client.Debt, Mode=OneWay}"
                   SpinButtonPlacementMode="Inline"
                   SmallChange="1" />

        
    </StackPanel>
</ContentDialog>
